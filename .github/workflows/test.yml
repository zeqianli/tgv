name: test

on:
  push:
    branches: [ "main" ]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run tests
      run: |
        echo "hihi" > src/snapshots/test.txt &&
        ls src/
      working-directory: .
    - name: Show snapshots
      run: ls src/
      working-directory: .
